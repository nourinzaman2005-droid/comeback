# MediaPipe browser runtime

The `wasm` directory is copied unchanged from `@mediapipe/tasks-vision` version 1.0.1. Generated runtime files are excluded from linting and served locally for the on-device pose worker.

Package documentation: https://www.npmjs.com/package/@mediapipe/tasks-vision
